[pytest]
testpaths =
    tests
    features

markers =
    youtube: downloads a real video from YouTube, which refuses datacenter addresses

addopts = --cov=EchoInStone --cov-report html --cov-report xml
